"Hi <%=session[:name]%>, would you like to choose Rock, Paper or Scissors?"

<form action='choose' method='post'>
  <ul class='unstyled'>
    <% Choices.each do |symp, text| %>
     	<li>
        <label class='radio'>
        <input type='radio' name='vote' value='<%= sym %>' id='vote_<%= sym %>' />
          <%= text %>
          </label>
      </li>
    <% end %>
  </ul>
  <button type='submit' class='btn btn-primary'>Play!</button>
</form>